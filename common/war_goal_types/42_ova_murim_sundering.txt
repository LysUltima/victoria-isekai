### Murim storyline - "Sundering of the Mountain" war goal
# kind = annex_country, mirrored by dp_ova_murim_reckoning (the bad-ending scripted war
# between the Returner's host and the Heavenly Demon Sect): whichever side wins annexes
# the other outright, as a civil war ends. If the host wins,
# je_ova_murim_reckoning's on_complete immediately runs effect_ova_murim_shatter_cult,
# which releases the Cult's five mountain states as the successor sects
# restoring the great orthodox factions the Cult destroyed (AMT Amita Sect / GON Gonryun
# Sect / JON Jongnam Sect / MOY Moyong Clan / GAE Gaebang), returning every conquered
# banner state to its original sect, and
# leaves the host whatever the Cult held outside the murim. Never player-selectable
# (possible = always no); only effect_ova_murim_launch_reckoning creates the play.

ova_murim_sundering = {
	icon = "gfx/interface/icons/war_goals/annex_country.dds"

	kind = annex_country

	settings = {
		require_target_be_part_of_war
		can_add_for_other_country
		annexes_entire_country
		skip_build_list

		# Validation
		validate_conflicts_war_goals_all
		validate_conflicts_annex_country
	}

	execution_priority = 80

	contestion_type = control_half_target_country_states

	target_type = country

	possible = {
		always = no
	}

	valid = {
	}

	maneuvers = {
		value = 0

		add = {
			desc = "MANEUVERS_BASE_VALUE"
			value = 10
		}
	}

	infamy = {
		value = 0
	}

	ai = {
		is_significant_demand = yes
	}
}
