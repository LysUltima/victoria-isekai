# Templates
shipname_prefixes_clone_template = {
	template = yes

	properties = {
        {
			type = custom_text # This country should not use ship prefixes
			key = "PREFIX"
			custom_text = "ship_prefix_blank"
		}
	}
}

### Fallbacks

# Generic, infinite fallbacks

shipname_fallback_ordinal_battleship = {
	name = "shipname_fallback_ordinal_battleship" # Battleship No. 1
	selection_weight = 1
	fallback = yes

	allowed_ship_types = {
		ship_type_early_ironclad
		ship_type_modern_ironclad
		ship_type_pre_dreadnought
		ship_type_dreadnought
		ship_type_super_dreadnought
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = ordinal_number
			key = "NUM"
		}
	}
}

shipname_fallback_ordinal_cruiser = {
	name = "shipname_fallback_ordinal_cruiser" # Cruiser No. 1
	selection_weight = 1
	fallback = yes

	allowed_ship_types = {
		ship_type_protected_cruiser
		ship_type_armored_cruiser
		ship_type_light_cruiser
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = ordinal_number
			key = "NUM"
		}
	}
}

shipname_fallback_ordinal_frigate = {
	name = "shipname_fallback_ordinal_frigate" # Frigate No. 1
	selection_weight = 1
	fallback = yes

	allowed_ship_types = {
		ship_type_frigate
		ship_type_iron_frigate
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = ordinal_number
			key = "NUM"
		}
	}
}

shipname_fallback_ordinal_monitor = {
	name = "shipname_fallback_ordinal_monitor" # Monitor No. 1
	selection_weight = 1
	fallback = yes

	allowed_ship_types = {
		ship_type_monitor
		ship_type_coastal_defense_ship
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = ordinal_number
			key = "NUM"
		}
	}
}

shipname_fallback_ordinal_troopship = {
	name = "shipname_fallback_ordinal_troopship" # Troopship No. 1
	selection_weight = 1
	fallback = yes

	allowed_ship_types = {
		ship_type_troop_ship
	}

	# Civilian ships should not use military prefixes (Fallback troopships are imagined as civilian transports pressed into military service )

	properties = {
		{
			type = ordinal_number
			key = "NUM"
		}
	}
}


shipname_fallback_ordinal_supply_ship = {
	name = "shipname_fallback_ordinal_supply_ship" # Transport No. 1
	selection_weight = 1
	fallback = yes

	allowed_ship_types = {
		ship_type_supply_ship
	}

	# Civilian ships should not use military prefixes (Fallback supply ships are imagined as civilian transports pressed into military service )

	properties = {
		{
			type = ordinal_number
			key = "NUM"
		}
	}
}

shipname_fallback_ordinal_torpedo_boat_flotilla = {
	name = "shipname_fallback_ordinal_tb_flotilla" # 1st Torpedo Boat Flotilla
	selection_weight = 1
	fallback = yes

	allowed_ship_types = {
		ship_type_torpedo_boat
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = ordinal_number
			key = "NUM"
		}
	}
}


shipname_fallback_ordinal_generic = {
	name = "shipname_fallback_ordinal_generic" # SHIP_TYPE No. 1
	selection_weight = 1
	fallback = yes

	allowed_ship_types = {
		ship_type_ship_of_the_line
		ship_type_seaplane_tender
		ship_type_aircraft_carrier
		ship_type_torpedo_boat_destroyer
		ship_type_destroyer
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = ship_type
			key = "SHIP_TYPE_NAME"
		}
		{
			type = ordinal_number
			key = "NUM"
		}
	}
}

shipname_fallback_ordinal_submarine = { # S-1
	name = "shipname_fallback_submarine"
	selection_weight = 1
	fallback = yes

	allowed_ship_types = {
		ship_type_submarine
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = ordinal_number
			key = "NUM"
		}
	}
}

# Higher priority fallbacks tied to gamestate
# Arranged in high/med/low definitions to have capital ships prioritize state names, cruisers hubs, etc.

shipname_fallback_torpedo_craft_high = { # Torpedo boats and destroyers derive their names from primary culture surnames
	name = "shipname_default_format"
	selection_weight = 5
	fallback = yes

	allowed_ship_types = {
		ship_type_torpedo_boat_destroyer
		ship_type_destroyer
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = culture_surnames
			key = "SHIP_NAMESAKE"
		}
	}
}

shipname_fallback_capital_ships_high = { # Capital ships derive their names from state names
	name = "shipname_default_format"
	selection_weight = 5
	fallback = yes

	allowed_ship_types = {
		ship_type_ship_of_the_line
		ship_type_early_ironclad
		ship_type_monitor
		ship_type_coastal_defense_ship
		ship_type_modern_ironclad
		ship_type_pre_dreadnought
		ship_type_dreadnought
		ship_type_super_dreadnought
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = owned_states
			key = "SHIP_NAMESAKE"
		}
	}
}

shipname_fallback_cruisers_high = { # Cruisers derive their names from Hub names
	name = "shipname_default_format"
	selection_weight = 5
	fallback = yes

	allowed_ship_types = {
		ship_type_frigate
		ship_type_iron_frigate
		ship_type_light_cruiser
		ship_type_protected_cruiser
		ship_type_armored_cruiser
		ship_type_seaplane_tender
		ship_type_aircraft_carrier
		ship_type_troop_ship
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = owned_hubs
			key = "SHIP_NAMESAKE"
		}
	}
}

shipname_fallback_torpedo_craft_medium = { # If surnames are exhaused, will be named after hub names
	name = "shipname_default_format"
	selection_weight = 4
	fallback = yes

	allowed_ship_types = {
		ship_type_torpedo_boat_destroyer
		ship_type_destroyer
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = owned_hubs
			key = "SHIP_NAMESAKE"
		}
	}
}

shipname_fallback_capital_ships_medium = { # If state names are exhaused, will be named after hub names
	name = "shipname_default_format"
	selection_weight = 4
	fallback = yes

	allowed_ship_types = {
		ship_type_ship_of_the_line
		ship_type_early_ironclad
		ship_type_monitor
		ship_type_coastal_defense_ship
		ship_type_modern_ironclad
		ship_type_pre_dreadnought
		ship_type_dreadnought
		ship_type_super_dreadnought
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = owned_hubs
			key = "SHIP_NAMESAKE"
		}
	}
}

shipname_fallback_cruisers_medium = { # If hub names are exhaused, will be named after states
	name = "shipname_default_format"
	selection_weight = 4
	fallback = yes

	allowed_ship_types = {
		ship_type_frigate
		ship_type_iron_frigate
		ship_type_light_cruiser
		ship_type_protected_cruiser
		ship_type_armored_cruiser
		ship_type_seaplane_tender
		ship_type_aircraft_carrier
		ship_type_troop_ship
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = owned_states
			key = "SHIP_NAMESAKE"
		}
	}
}

shipname_fallback_torpedo_craft_low = { # If surnames and  hub names are exhaused, will be named after states
	name = "shipname_default_format"
	selection_weight = 3
	fallback = yes

	allowed_ship_types = {
		ship_type_torpedo_boat_destroyer
		ship_type_destroyer
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = owned_states
			key = "SHIP_NAMESAKE"
		}
	}
}

shipname_fallback_capital_ships_low = { # If state names and hub names are exhaused, will be named after surnames
	name = "shipname_default_format"
	selection_weight = 3
	fallback = yes

	allowed_ship_types = {
		ship_type_ship_of_the_line
		ship_type_early_ironclad
		ship_type_monitor
		ship_type_coastal_defense_ship
		ship_type_modern_ironclad
		ship_type_pre_dreadnought
		ship_type_dreadnought
		ship_type_super_dreadnought
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = culture_surnames
			key = "SHIP_NAMESAKE"
		}
	}
}

shipname_fallback_cruisers_low = { # If hub names are exhaused and state names, will be named after surnames
	name = "shipname_default_format"
	selection_weight = 3
	fallback = yes

	allowed_ship_types = {
		ship_type_frigate
		ship_type_iron_frigate
		ship_type_light_cruiser
		ship_type_protected_cruiser
		ship_type_armored_cruiser
		ship_type_seaplane_tender
		ship_type_aircraft_carrier
		ship_type_troop_ship
	}

	clone_properties = shipname_prefixes_clone_template

	properties = {
		{
			type = culture_surnames
			key = "SHIP_NAMESAKE"
		}
	}
}
