# NB: When altering any triggers here, make sure to mirror those changes in japan_custom_loc to avoid trigger dissonance
tenpo_hardliner_goals_completed = {
	value = 0
	if = { # Tenpo - Hardliner: Social Control
		limit = {
			has_law_or_variant = law_type:law_secret_police
		}
		add = 1
	}
	if = { # Tenpo - Hardliner: Edo System: Strict
		limit = {
			has_law_or_variant = law_type:law_strict_edo_system
		}
		add = 1
	}
	if = { # Tenpo - Hardliner: PB Marginalized
		limit = {
            ig:ig_petty_bourgeoisie = {
                is_marginal = yes
            }
		}
		add = 1
	}
	if = { # Tenpo - Hardliner: Rural
		limit = {
            country_has_building_type_levels = {
                target = bt:building_rice_farm
                value >= 40
            }
		}
		add = 1
	}
}

tenpo_reformer_goals_completed = {
	value = 0
	if = { # Tenpo - Reformer: Urbanization
		limit = {
            any_scope_state = {
                state_region = {
                    is_homeland = cu:sakura
                }
                any_scope_building = {
                    is_building_type = building_urban_center
                    level >= 3
                }
                count >= 3
            }
		}
		add = 1
	}
	if = { # Tenpo - Reformer: Edo System: Lax
		limit = {
			has_law_or_variant = law_type:law_lax_edo_system
		}
		add = 1
	}
	if = { # Tenpo - Reformer: PB Clout
		limit = {
            ig:ig_petty_bourgeoisie = {
                ig_clout >= 0.1
            }
		}
		add = 1
	}
	if = { # Tenpo - Reformer: Bureaucrats
		limit = {
            NOT = {
                has_law_or_variant = law_type:law_hereditary_bureaucrats
            }
		}
		add = 1
	}
}

tenpo_total_agenda_goals_completed = {
	value = 0
	# Hardliner Agendas
	if = { # Tenpo - Hardliner: Social Control
		limit = {
			has_law_or_variant = law_type:law_secret_police
		}
		add = 1
	}
	if = { # Tenpo - Hardliner: Edo System: Strict
		limit = {
			has_law_or_variant = law_type:law_strict_edo_system
		}
		add = 1
	}
	if = { # Tenpo - Hardliner: PB Marginalized
		limit = {
            ig:ig_petty_bourgeoisie = {
                is_marginal = yes
            }
		}
		add = 1
	}
	if = { # Tenpo - Hardliner: Rural
		limit = {
            country_has_building_type_levels = {
                target = bt:building_rice_farm
                value >= 40
            }
		}
		add = 1
	}
	# Reformer Agendas
	if = { # Tenpo - Reformer: Urbanization
		limit = {
            any_scope_state = {
                state_region = {
                    is_homeland = cu:sakura
                }
                any_scope_building = {
                    is_building_type = building_urban_center
                    level >= 3
                }
                count >= 3
            }
		}
		add = 1
	}
	if = { # Tenpo - Reformer: Edo System: Lax
		limit = {
			has_law_or_variant = law_type:law_lax_edo_system
		}
		add = 1
	}
	if = { # Tenpo - Reformer: PB Clout
		limit = {
            ig:ig_petty_bourgeoisie = {
                ig_clout >= 0.1
            }
		}
		add = 1
	}
	if = { # Tenpo - Reformer: Bureaucrats
		limit = {
            NOT = {
                has_law_or_variant = law_type:law_hereditary_bureaucrats
            }
		}
		add = 1
	}
	# Shared Agendas
	if = { # Tenpo - Shared: Army Size
		limit = {
			army_power_projection > 500
		}
		add = 1
	}
	if = { # Tenpo - Shared: Naval
		limit = {
            country_has_building_type_levels = {
                target = bt:building_naval_fortification
                value >= 10
            }
		}
		add = 1
	}
	if = { # Tenpo - Shared: Famine
		limit = {
            NOT = {
                any_scope_state = {
                    state_region = {
                        is_homeland = cu:sakura
                    }
                    has_famine = yes
                }
            }
		}
		add = 1
	}
}

absolute_state_daimyo_loyalty = { # var:cached_daimyo_loyalty is never set (its only writer depended on
	value = 0.5 # dead var:daimyo_var); always neutral
}

state_daimyo_loyalty = { # var:cached_daimyo_loyalty is never set (its only writer depended on
	value = 0.5 # dead var:daimyo_var); always neutral
}

state_daimyo_loyalty_impact_restorationist = {
	value = 1.5
	subtract = state_daimyo_loyalty
	subtract = 1
}

state_daimyo_loyalty_impact_tokugawa = {
	value = 0.5
	add = state_daimyo_loyalty
	subtract = 1
}

patronize_authority_cost = { # In character scope
	value = 100 # Baseline
	if = {
		limit = {
			OR = {
				has_trait = opium_addiction
				has_trait = alcoholic
				has_trait = cocaine_addiction
				has_trait = expensive_tastes
			}
		}
		add = 50
	}
}

ostracize_authority_cost = { # In character scope
	value = 100 # Baseline
}

political_concession_level_1_auth = 50
political_concession_level_2_auth = 100
political_concession_level_3_auth = 150
political_concession_level_1_auth_cost = { value = political_concession_level_1_auth multiply = -1 }
political_concession_level_2_auth_cost = { value = political_concession_level_2_auth multiply = -1 }
political_concession_level_3_auth_cost = { value = political_concession_level_3_auth multiply = -1 }

retire_daimyo_authority_cost = 100
peerage_authority_cost = 25

country_levels_owned_by_zaibatsu = {
	value = 0
	if = {
		limit = {
			any_company = {
				company_is_zaibatsu = yes
			}
		}
		every_company = {
			limit = {
				company_is_zaibatsu = yes
			}
			add = this.company_owned_levels
		}
	}
}

fraction_of_country_levels_owned_by_zaibatsu = {
	if = {
		limit = {
			country_levels_owned_by_zaibatsu > 0
		}
		value = this.country_levels_owned_by_zaibatsu
		divide = this.building_levels
	}
	else = {
		value = 0
	}
}

# je_zaibatsu thresholds — NB: if altering these, update ep2_04_l_english.yml tooltip strings to match
# removed effect line: vanilla ref 'je_zaibatsu_company_slots_threshold' not present in this mod
# removed effect line: vanilla ref 'je_zaibatsu_min_company_count' not present in this mod
# removed effect line: vanilla ref 'je_zaibatsu_possible_levels_threshold' not present in this mod
# removed effect line: vanilla ref 'je_zaibatsu_possible_fraction_threshold' not present in this mod
# removed effect line: vanilla ref 'je_zaibatsu_complete_levels_threshold' not present in this mod
# removed effect line: vanilla ref 'je_zaibatsu_complete_fraction_threshold' not present in this mod
# removed effect line: vanilla ref 'je_zaibatsu_fail_legitimacy_threshold' not present in this mod
# removed effect line: vanilla ref 'je_zaibatsu_fail_voting_power_threshold' not present in this mod
# removed effect line: vanilla ref 'je_zaibatsu_electoral_confidence_bonus' not present in this mod

state_gdp_per_capita = {
	value = gdp
	if = {
		limit = {
			state_population >= 0
		}
		divide = state_population
	}
	else = {
		divide = 1
	}
}
