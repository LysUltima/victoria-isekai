# Sect subject pact. No income_transfer: subordinate sects owe allegiance, not tribute.

sect = {
	groups = {
		general # for breaking only
	}
	show_in_lens = no

	potential = { # Only created through diplomatic plays, sway offers, or history
		always = no
	}

	pact = {
		cost = 10
		has_junior_participant = yes
		maintenance_paid_by = first_country

		max_target_involvement = 8000
		target_involvement_applies_to = source_country

		relations_progress_per_day = 1
		relations_improvement_max = 50

		subject_type = subject_type_sect

		second_modifier = {}

		actor_can_break = {
			is_diplomatic_play_committed_participant = no
		}

		target_can_break = { # Has to use diplomatic play to break
			always = no
		}

		manual_break_effect = {
			create_bidirectional_truce = { country = scope:target_country months = 60 }
		}

		auto_break_effect = {
			create_bidirectional_truce = { country = scope:target_country months = 60 }
		}
	}

	ai = {
		will_break = {
			always = no
		}
	}
}
