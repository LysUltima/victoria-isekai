ig_armed_forces = {
	color = hsv{ 0.09 0.29 0.39 }
	texture = "gfx/interface/icons/ig_icons/armed_forces.dds"
	layer = "revolution_dynamic_armed_forces"
	index = 0

	ideologies = {
		ideology_jingoist
		ideology_loyalist
		ideology_patriotic
	}

	character_ideologies = {
		ideology_moderate
		ideology_authoritarian
	}

	enable = {
		always = yes
	}

	# Deprecated; use on_enable effect to assign traits instead
	traits = {
		ig_trait_patriotic_fervor
		ig_trait_veteran_consultation
		ig_trait_materiel_waste
	}

	on_enable = {
		ig:ig_armed_forces ?= {
			if = {
				limit = {
					owner = {
						always = no # vanilla ref 'c:CHI' not present in this mod
					}
				}
				set_ig_trait = ig_trait:ig_trait_newly_created_army
				set_ig_trait = ig_trait:ig_trait_self_strengthening
				set_ig_trait = ig_trait:ig_trait_parochial_leadership
			}
			else_if = {
				limit = {
					owner = {
						OR = {
							always = no # vanilla ref 'c:FRA' not present in this mod
							country_has_primary_culture = cu:solenne # was cu:french
						}
					}
				}
				set_ig_trait = ig_trait:ig_trait_materiel_waste
				set_ig_trait = ig_trait:ig_trait_veteran_consultation
				set_ig_trait = ig_trait:ig_trait_elan_vital
			}
			else_if = {
				limit = {
					owner = {
						any_primary_culture = {
							has_discrimination_trait = language_hispanophone
							has_discrimination_trait_group = heritage_group_european
							NOT = { has_discrimination_trait = heritage_iberian }
						}
					}
				}
				set_ig_trait = ig_trait:ig_trait_materiel_waste
				set_ig_trait = ig_trait:ig_trait_veteran_consultation
				set_ig_trait = ig_trait:ig_trait_el_buen_jefe
			}
			else_if = {
				limit = {
					owner = {
						has_dlc_feature = rp1_content
						always = no # vanilla ref 'c:BRZ' not present in this mod
					}
				}
				set_ig_trait = ig_trait:ig_trait_coronelismo
				set_ig_trait = ig_trait:ig_trait_clube_militar
				set_ig_trait = ig_trait:ig_trait_patriotic_fervor
			}
			else_if = {
				limit = {
					owner = {
						has_dlc_feature = ip2_content
						always = no # vanilla ref 'c:BIC' not present in this mod
					}
				}
				set_ig_trait = ig_trait:ig_trait_divided_ranks
				set_ig_trait = ig_trait:ig_trait_imperial_service
				set_ig_trait = ig_trait:ig_trait_veerta
			}
			else_if = {
				limit = {
					owner = {
						always = no # vanilla ref 'c:PAN' not present in this mod
					}
				}
				set_ig_trait = ig_trait:ig_trait_materiel_waste
				set_ig_trait = ig_trait:ig_trait_khalsa
				set_ig_trait = ig_trait:ig_trait_patriotic_fervor
			}
			else_if = {
				limit = {
					owner = {
						always = no # vanilla ref 'c:ZAN' not present in this mod
						any_primary_culture = { cu:zaharin = this} # was cu:bedouin
					}
				}
				set_ig_trait = ig_trait:ig_trait_divided_ranks
				set_ig_trait = ig_trait:ig_trait_veteran_consultation
				set_ig_trait = ig_trait:ig_trait_patriotic_fervor
			}
			else = {
				set_ig_trait = ig_trait:ig_trait_materiel_waste
				set_ig_trait = ig_trait:ig_trait_veteran_consultation
				set_ig_trait = ig_trait:ig_trait_patriotic_fervor
			}
		}
		# Caudillo Ideology
		if = {
			limit = {
				always = no # vanilla ref 'geographic_region_latin_america' not present in this mod
				OR = {
					country_has_primary_culture = cu:alena # was cu:platinean
					country_has_primary_culture = cu:alena # was cu:south_andean
					country_has_primary_culture = cu:alena # was cu:north_andean
					country_has_primary_culture = cu:alena # was cu:central_american
					country_has_primary_culture = cu:alena # was cu:mexican
				}
			}
			add_modifier = age_of_caudillos_modifier
			ig:ig_armed_forces ?= {
				add_ideology = ideology_caudillismo
			}
		}
		# Samurai
		else_if = {
			limit = {
				country_has_primary_culture = cu:sakura # was cu:japanese
				NOT = { always = no }
			}
			ig:ig_armed_forces ?= {
				add_ideology = ideology_bakufu
				set_interest_group_name = ig_samurai
			}
		}
		# India
		# Sikh Empire
	}
	on_disable = {}

	priority_cultures = { #Country scope
		rule = {
			trigger = {
				always = no # vanilla ref 'c:BIC' not present in this mod
				any_primary_culture = {
					cu:aldrican = this # was cu:british
				}
			}
			cultures = {}
		}
		rule = {
			trigger = {
				always = no # vanilla ref 'c:ZAN' not present in this mod
				any_primary_culture = {
					cu:zaharin = this # was cu:bedouin
				}
			}
			cultures = {}
		}
		rule = {
			trigger = {
				always = no # vanilla ref 'c:PHI' not present in this mod
				NOT = { always = no} # vanilla ref 'je_philippines_main_reliance_happened' not present in this mod
				NOT = { any_primary_culture = { cu:melati = this } }
				NOT = { any_primary_culture = { cu:melati = this } }
				any_primary_culture = { cu:alena = this} # was cu:spanish
			}
			cultures = {}
		}
		rule = {
			trigger = {
				always = no # vanilla ref 'c:PHI' not present in this mod
				any_primary_culture = {
					AND = {
						cu:alena = this # was cu:spanish
						cu:melati = this # was cu:filipino_hispanophone
					}
				}
			}
			cultures = {}
		}
	}

	on_character_ig_membership = {}

	pop_potential = {
		OR = {
			is_pop_type = soldiers
			is_pop_type = officers
			is_pop_type = aristocrats
			pop_acceptance >= acceptance_status_4
		}
	}

	pop_weight = {
		value = 0

		add = {
			desc = "POP_NOT_DISCRIMINATED"
			if = {
				limit = {
					NOT = {
						owner = {
							has_social_hierarchy = edo_system
						}
					}
					pop_acceptance >= acceptance_status_4
				}
				value = 25
				if = { # Militarisation of colonial settlers
					limit = {
						owner = {
							OR = {
								has_law_or_variant = law_type:law_colonial_resettlement
								has_law_or_variant = law_type:law_frontier_colonization
							}
						}
						state = {
							is_incorporated = no
						}
					}
					add = 50
				}
				if = { # Separation of the military and people
					limit = {
						NOR = {
							is_pop_type = soldiers
							is_pop_type = officers
						}
						owner = {
							has_law_or_variant = law_type:law_professional_army
						}
					}
					divide = 2
				}
			}
		}

		add = {
			desc = "POP_SOLDIERS"
			if = {
				limit = {
					is_pop_type = soldiers
				}
				value = 100
				if = { # Institutional loyalty
					limit = {
						owner = {
							has_law_or_variant = law_type:law_professional_army
						}
					}
					add = 50
				}
				else_if = {
					limit = {
						owner = {
							has_law = law_type:law_warrior_caste
						}
					}
					add = 100
				}
				if = { # Pay makes soldiers more inclined to be loyal to Armed Forces
					limit = {
						owner = {
							military_wage_level = very_low
						}
					}
					multiply = 0.75
				}
				if = {
					limit = {
						owner = {
							military_wage_level = low
						}
					}
					multiply = 0.85
				}
				if = {
					limit = {
						owner = {
							military_wage_level = medium
						}
					}
					multiply = 1
				}
				if = {
					limit = {
						owner = {
							military_wage_level = high
						}
					}
					multiply = 1.15
				}
				if = {
					limit = {
						owner = {
							military_wage_level = very_high
						}
					}
					multiply = 1.25
				}
			}
		}

		add = {
			desc = "POP_ARISTOCRATS"
			if = {
				limit = {
					is_pop_type = aristocrats
				}
				value = 5
				if = {
					limit = {
						owner = {
							has_modifier = age_of_caudillos_modifier
						}
					}
					add = 25
				}
				if = {
					limit = {
						owner = {
							has_law = law_type:law_peasant_levies
						}
					}
					add = 50
				}
			}
		}

		add = {
			desc = "SAMURAI_TRADITIONS" # Warrior caste
			if = {
				limit = {
					is_pop_type = aristocrats
					culture = cu:sakura # was cu:japanese
				}
				add = 100
			}
		}

		add = {
			desc = "SIKH_MILITANCY"
			if = {
				limit = {
					is_pop_type = aristocrats
					religion = rel:verdant_sutra # was rel:sikh
				}
				add = 100
			}
		}

		add = {
			desc = "POP_OFFICERS"
			if = {
				limit = {
					is_pop_type = officers
				}
				value = 250
			}
		}

		add = { # There's one firearm for every twelve people on the planet. The question is, how do we arm the other eleven?
			desc = "MILITARY_INDUSTRIES"
			if = {
				limit = {
					pop_employment_building_group = bg_military_industry
				}
				if = {
					limit = {
						strata = middle
					}
					value = 75
				}
				if = {
					limit = {
						strata = upper
					}
					value = 150
				}
			}
		}

		add = {
			desc = "POP_MILITARY_BUREAUCRACY"
			if = {
				limit = {
					is_pop_type = bureaucrats
					owner = {
						always = no # vanilla ref 'c:ALK' not present in this mod
						has_government_type = gov_chartered_company
					}
				}
				value = 100
			}
		}

		multiply = {
			desc = "LEADER_POPULARITY"
			scope:interest_group ?= {
				leader ?= {
					value = popularity
					multiply = 0.0025
					add = 1
					max = 1.25
					min = 0.75
				}
			}
		}
		if = {
			limit = {
				scope:interest_group ?= {
					is_in_government = yes
				}
			}
			multiply = {
				desc = "IN_GOVERNMENT_ATTRACTION"
				value = 1
				add = scope:interest_group.modifier:interest_group_in_government_attraction_mult
				min = 0
			}
		}
		if = {
			limit = {
				scope:interest_group ?= {
					is_in_government = yes
				}
			}
			multiply = { # Multiplied by 1.25x max
				desc = "POP_LOYALISTS"
				value = pop_loyalist_fraction
				divide = 4
				add = 1
			}
		}
		if = {
			limit = {
				scope:interest_group ?= {
					is_in_government = no
				}
			}
			multiply = { # Multiplied by 1.25x max
				desc = "POP_RADICALS"
				value = pop_radical_fraction
				divide = 4
				add = 1
			}
		}
	}

	monarch_weight = {
		value = 1
		# Monarch is not likely to adopt a marginal IG
		if = {
			limit = {
				ig_counts_as_marginal = yes
			}
			multiply = {
				value = 0.1
			}
		}
		# Monarch is more likely to adopt a Powerful IG
		if = {
			limit = {
				is_powerful = yes
			}
			multiply = {
				value = 2
			}
		}
		# Monarch more likely to be Armed Forces under an autocratic or oligarchic regime
		if = {
			limit = {
				owner = {
					OR = {
						has_law_or_variant = law_type:law_oligarchy
						has_law_or_variant = law_type:law_autocracy
						has_law_or_variant = law_type:law_single_party_state
					}
				}
			}
			multiply = {
				value = 2
			}
		}
		# Monarch is less likely to be Armed forces in a democratic monarchy
		if = {
			limit = {
				owner = {
					OR = {
						has_law_or_variant = law_type:law_universal_suffrage
						has_law_or_variant = law_type:law_census_voting
					}
				}
			}
			multiply = {
				value = 0.5
			}
		}
		# Less likely without Professional Army or Mass Conscription
		if = {
			limit = {
				owner = {
					NOR = {
						has_law_or_variant = law_type:law_professional_army
						has_law_or_variant = law_type:law_mass_conscription
					}
				}
			}
			multiply = {
				value = 0.5
			}
		}
	}

	agitator_weight = {
		# Agitators more likely to come from populist IG's rather than elitist
		value = 0.5
		# Agitators will never be part of a marginalized IG
		if = {
			limit = {
				owner.ig:ig_armed_forces ?= {
					ig_counts_as_marginal = yes
				}
			}
			multiply = {
				value = 0
			}
		}
	}

	commander_weight = {
		value = 5.0
	}
	
	magnate_weight = {
		value = 1.0
	}	

	executive_weight = {
		value = 0.2

		# The company owning BIC should not get non-industrialist executives
		if = {
			limit = {
				exists = scope:company
				always = no # vanilla ref 'c:BIC' not present in this mod
				always = no # blanked: vanilla ref 'c:BIC' not present in this mod
			}
			multiply = {
				value = 0
			}
		}
	}

	noble_chance = {
		value = 0.5
	}

	female_commander_chance = {
		value = 0.2

		# Matriarchal branch: only women lead
		if = {
			limit = {
				owner = {
					ova_country_is_matriarchal_society = yes
				}
			}
			value = 1.0
		}
	}

	female_executive_chance = {
		value = 0.0
		# Matriarchal branch: only women lead
		if = {
			limit = {
				owner = {
					ova_country_is_matriarchal_society = yes
				}
			}
			value = 1.0
		}
	}

	female_monarch_chance = {
		value = 0
		# Matriarchal branch: only women lead
		if = {
			limit = {
				owner = {
					ova_country_is_matriarchal_society = yes
				}
			}
			value = 1.0
		}
	}

	female_politician_chance = {
		value = 0.0

		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_womens_suffrage
				}
				law_stance = {
					law = law_type:law_womens_suffrage
					value >= approve
				}
			}
			add = {
				value = 0.025
			}
		}
		# Matriarchal branch: only women lead
		if = {
			limit = {
				owner = {
					ova_country_is_matriarchal_society = yes
				}
			}
			value = 1.0
		}

		if = {
			limit = {
				owner = {
					has_law = law_type:law_mens_suffrage
				}
				law_stance = {
					law = law_type:law_mens_suffrage
					value >= approve
				}
			}
			value = 0.975
		}
	}

	female_agitator_chance = {
		value = 0.01

		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_women_own_property
				}

			}
			add = {
				value = 0.01
			}
		}

		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_women_in_the_workplace
				}

			}
			add = {
				value = 0.01
			}
		}

		if = {
			limit = {
				owner = {
					has_law_or_variant = law_type:law_womens_suffrage
				}

			}
			add = {
				value = 0.01
			}
		}
		# Matriarchal branch: only women lead
		if = {
			limit = {
				owner = {
					ova_country_is_matriarchal_society = yes
				}
			}
			value = 0.99
		}

		if = {
			limit = {
				owner = {
					OR = {
						has_law = law_type:law_men_own_property
						has_law = law_type:law_men_in_the_workplace
						has_law = law_type:law_mens_suffrage
					}
				}
			}
			subtract = 0.01
		}
	}

	# Multiplier applied to the weight for an appropriate commander will be selected from the character pool to lead the IG
	# root = interest group
	commander_leader_weight = {
		value = 3.0
	}
	
	# Multiplier applied to the weight for an appropriate magnate will be selected from the character pool to lead the IG
	# root = interest group
	magnate_leader_weight = {
		value = 1.0
		if = {
			limit = {
				always = no # vanilla ref 'c:JAP' not present in this mod
				NOT = {
					always = no
				}
			}
			add = 5
		}
	}

	# Multiplier applied to the weight for an appropriate executive will be selected from the character pool to lead the IG
	# root = interest group
	executive_leader_weight = {
		value = 0.1
	}
}
